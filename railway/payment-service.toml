[build]
dockerfilePath = "services/Dockerfile.template"
buildArgs = { SERVICE_DIR = "services/payment-service" }

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
