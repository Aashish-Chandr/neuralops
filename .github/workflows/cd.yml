name: CD — Build, Push, Deploy

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/neuralops

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    strategy:
      matrix:
        include:
          - name: inference-server
            context: ml
            dockerfile: ml/Dockerfile
          - name: metrics-exporter
            context: streaming
            dockerfile: streaming/Dockerfile
          - name: remediation-engine
            context: remediation
            dockerfile: remediation/Dockerfile
          - name: drift-detector
            context: drift
            dockerfile: drift/Dockerfile
          - name: frontend
            context: frontend
            dockerfile: frontend/Dockerfile
          - name: api-gateway
            context: frontend/api-gateway
            dockerfile: frontend/api-gateway/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}/${{ matrix.name }}:latest
            ${{ env.IMAGE_PREFIX }}/${{ matrix.name }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  update-helm-values:
    runs-on: ubuntu-latest
    needs: build-and-push
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update image tags in Helm values
        run: |
          SHA=${{ github.sha }}
          sed -i "s|tag: .*|tag: ${SHA}|g" infra/helm/neuralops/values.yaml

      - name: Commit updated values
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add infra/helm/neuralops/values.yaml
          git commit -m "chore: update image tags to ${{ github.sha }} [skip ci]" || echo "No changes"
          git push
